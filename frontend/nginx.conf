server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Kamal proxy health check
    location = /up {
        access_log off;
        add_header Content-Type text/plain;
        return 200 'OK';
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
